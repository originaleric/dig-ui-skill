# 安装器复制清单补齐与本地安装过滤落地

## 更新内容

- 复查 `bin/dig-ui-skill.mjs` 中的 `SKILL_TOP_LEVEL_FILES` 与 `SKILL_DIRS`，确认它们落后于当前 `package.json` 发布清单。
- 补齐安装器顶层文件复制清单：
  - `README.zh-CN.md`
  - `INSTALL.md`
  - `LICENSE`
  - `validate-dig-catalog-preview.mjs`
- 补齐安装器目录复制清单：新增 `adapters/`，保证 Cursor project rule 模板等适配资产会随 skill 安装同步。
- 保持 `react/` 不恢复。历史确认被删除的是独立的 Three.js 3D Logo Loader，不是当前 Dig UI prompt-as-code 主线所需的 React 组件层。
- 新增安装复制过滤：
  - 跳过 `.DS_Store`
  - 跳过 `references/global-rules.local.md`
- 保持 `references/global-rules.local.md` 的保护语义：已有目标文件不会被覆盖，且本地仓库 `--source .` 安装时也不会把个人 local rules 复制出去。
- 同步更新 `README.zh-CN.md`，补充 `validate-dig-catalog-preview.mjs` 的目录结构说明和 `npm run validate:catalogs` 本地开发命令。
- 同步更新 `INSTALL.md` 的 update 覆盖清单，明确 catalog/layout validator、中文 README 与 INSTALL 文档会随标准资产更新。

## 验证

- `env npm_config_cache=/tmp/dig-ui-skill-npm-cache npm pack --dry-run`
- `HOME=/tmp/dig-ui-skill-install-check node bin/dig-ui-skill.mjs install codex --source .`
- `npm run validate:catalogs`
- `npm run validate:layouts`

验证结果：

- npm dry-run 发布清单包含 `validate-dig-catalog-preview.mjs` 与 `adapters/cursor/dig-ui.mdc`。
- 临时 Codex skill 安装目录包含 `README.zh-CN.md`、`INSTALL.md`、`LICENSE`、`validate-dig-catalog-preview.mjs` 与 `adapters/`。
- 临时安装目录未复制 `.DS_Store` 与 `references/global-rules.local.md`。
- Catalog render validation passed for 73 preview(s).
- Dig Layout Preview Validator：20 file(s)，0 FAIL，0 WARN。
