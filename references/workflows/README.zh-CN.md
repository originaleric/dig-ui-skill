# Dig Workflows

Workflow references 定义 agent 在 Dig UI 任务中的执行顺序。

## 可用流程

- `review.md`：按 Dig 规则审查已有 UI。
- `redesign.md`：在保留产品意图的前提下改进界面。
- `execution.md`：生成或审查 execution、runtime、observability、agent run 页面。
- `image-reference.md`：使用视觉参考，但不复制无关风格系统。
