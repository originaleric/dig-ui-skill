# Review Workflow

1. 读取 global rules。
2. 如存在，读取 local rules 与 local extensions。
3. 识别目标 layout、catalog、blocks。
4. 先检查 anti-tells，再讨论视觉细节。
5. 可用时运行 preflight 与 render validation。
6. 按严重程度输出 findings，并引用具体文件或资产。
